apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bitcoinbot.fullname" . }}-test-connection"
  labels:
    {{- include "bitcoinbot.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "bitcoinbot.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
