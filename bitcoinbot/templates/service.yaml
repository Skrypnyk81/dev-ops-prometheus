apiVersion: v1
kind: Service
metadata:
  name: {{ include "bitcoinbot.fullname" . }}
  labels:
    {{- include "bitcoinbot.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "bitcoinbot.selectorLabels" . | nindent 4 }}
